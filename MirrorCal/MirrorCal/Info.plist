<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- ============================================== -->
    <!-- CRITICAL: LSUIElement - Menu Bar Only App     -->
    <!-- ============================================== -->
    <!-- 
        This makes the app an "Agent" application:
        - No Dock icon
        - No main window
        - Runs entirely from the Menu Bar
    -->
    <key>LSUIElement</key>
    <true/>
    
    <!-- ============================================== -->
    <!-- CALENDAR PERMISSIONS (EventKit)               -->
    <!-- ============================================== -->
    <!-- 
        Required for requestFullAccessToEvents() on macOS 14+
        This is the privacy description shown to the user when
        requesting calendar access.
    -->
    <key>NSCalendarsFullAccessUsageDescription</key>
    <string>MirrorCal needs access to your calendars to read events from your source calendars and create mirrored events in your Mirror calendar. All data stays on your device.</string>
    
    <!-- ============================================== -->
    <!-- APP METADATA                                  -->
    <!-- ============================================== -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    
    <key>CFBundleVersion</key>
    <string>1</string>
    
    <!-- ============================================== -->
    <!-- LAUNCH AT LOGIN SUPPORT (Optional, for later) -->
    <!-- ============================================== -->
    <!-- 
        Uncomment when implementing Launch at Login:
        <key>LSBackgroundOnly</key>
        <false/>
    -->
    
</dict>
</plist>
